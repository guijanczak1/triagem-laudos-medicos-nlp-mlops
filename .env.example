# Copy this file to .env and fill in values as needed.
# All keys start empty on purpose - safe defaults live in code (src/triagem/config.py).
# Never commit a .env file with real values.

DATA_PATH=
MODELS_DIR=
MODEL_BACKEND=
LOG_LEVEL=
API_PORT=
