# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
build/
dist/

# Ferramentas / cache
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
htmlcov/

# Dados / modelos (grandes, reprodutíveis via script)
data/raw/
data/processed/
models/*.joblib
models/*.onnx

# Ambiente / segredos
.env
.env.*
!.env.example

# Terraform — binários e estado local; o lock file É versionado
deploy/.terraform/
deploy/*.tfstate
deploy/*.tfstate.*
deploy/.terraform.tfstate.lock.info

# Airflow (execução local)
airflow/logs/
airflow/airflow.db
airflow/airflow.cfg

# SO / editor
.DS_Store
Thumbs.db
.idea/
.vscode/
